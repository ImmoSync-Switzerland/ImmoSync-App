mod frb_generated; /* AUTO INJECTED BY flutter_rust_bridge. This line may not be accurate, and you can change it according to your needs. */
// Minimal scaffold for a Rust bridge to `matrix-rust-sdk`.
//
// This crate provides a FRB-compatible `bridge.rs` that exposes a tiny API
// surface to Dart. The actual matrix client implementation is left as an
// exercise — the bridge functions are the integration points.

pub mod bridge;
