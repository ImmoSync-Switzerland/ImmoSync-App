###############################################
# Android / Flutter module .gitignore (trimmed)
# Wrapper files are intentionally INCLUDED for CI.
###############################################
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java

# Keystore material (never commit real keys)
key.properties
**/*.keystore
**/*.jks

# Ensure Gradle wrapper scripts & JAR are tracked
!gradle-wrapper.jar
!/gradlew
!/gradlew.bat
